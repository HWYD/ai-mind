# AI Mind Project Assistant Service 生产环境变量模板
# 复制为 /srv/ai-mind/env/project-assistant-service.env，填写真实值后：
#   chmod 600 /srv/ai-mind/env/project-assistant-service.env
#
# 说明：本模板不包含任何真实 Token。
# Token 由部署人员在服务器本地填写，不通过 GitHub Actions 分发。

# MCP Token：必须与 webapp.env 中的 PROJECT_ASSISTANT_SERVICE_MCP_TOKEN 完全一致
# 生成方式：uuidgen 或 openssl rand -hex 32
PROJECT_ASSISTANT_SERVICE_MCP_TOKEN=CHANGE_ME
