# 复制为 deploy/env/project-assistant-service.local.env 后再填写真实值。
# Compose 会注入 host、port 和 TZ；这里主要保留本地 Secret 边界。

# 必须与 deploy/env/webapp.local.env 中保持同一个随机值。
PROJECT_ASSISTANT_SERVICE_MCP_TOKEN=replace-with-the-same-random-token-as-webapp
