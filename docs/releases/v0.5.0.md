# v0.5.0 发布记录

**状态**：实现已收口；内部预览分发仍等待运营验收

## 范围

为既有 AI Mind Web 应用提供 Windows x64 与 macOS arm64 的 Electron 在线宿主。应用使用固定的受信 HTTPS Origin、严格的 compatibility 准入、本地恢复页、隔离的浏览器 profile 和收紧的原生权限；打包边界仅限未签名的内部预览。

## 不包含的能力

- 公开桌面发行、Developer ID 签名、公证或自动更新
- macOS Intel x64 或 universal binary
- 本地/离线 AI Runtime、数据库、模型、MCP、Tool、Skill 或 Agent 的迁移
- 可编辑服务地址、HTTP/TLS 绕过、诊断上传或通用 IPC/文件 API

## 交付与回退

compatibility API 与 Web security headers 只能通过既有服务端部署链路上线。完成同一 commit 的生产验证后，内部发布人员才可通过受控渠道提供未签名的对应平台安装器或 DMG、manifest、SHA-256 和发布说明。

同一产品的手动覆盖安装会保留桌面 profile。macOS 首次启动仅按文档使用 Finder 的“右键打开”流程；ad-hoc signing 只是为了本地可执行性，不代表发布者签名。

若服务端必须回退到不含 compatibility/header contract 的版本，必须先暂停受影响的预览分发。已安装客户端将停留在本地恢复页，不接受替代 Origin、HTTP fallback 或远程升级 URL。

## 验证状态

仓库级验证记录在 v0.5.0 验收台账中。任何预览制品生成或分发前，仍须完成生产 compatibility 验证，以及 Windows x64 与 macOS arm64 的 fresh install / overlay install smoke。

## 参考资料

- [验收台账](../../specs/v0.5.0-electron-desktop-host/acceptance.md)
- [预览发布契约](../../specs/v0.5.0-electron-desktop-host/contracts/desktop-preview-release.md)
