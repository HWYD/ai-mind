# v0.2.2 Tasklist

- [x] Major Step 1：双应用 Docker 构建边界与运行时资产纳入
- [x] Major Step 2：本地 Docker Compose 与生产 env 示例收口
- [x] Major Step 3：生产 Compose、宿主机 Nginx 模板与部署脚本
- [x] Major Step 4：真实腾讯云手动部署验收
- [ ] Major Step 5：GitHub Actions 自动构建、推送 TCR 与 SSH 远端部署首轮跑通
- [ ] Major Step 6：生产回滚演练、更多 smoke 与运维证据补齐

## Real Delivery Notes

- 已确认生产路线从 GHCR 调整为腾讯云 TCR。
- 已确认 HTTPS 证书从 Certbot-only 调整为宿主机证书管理，当前使用腾讯云 SSL 手动部署。
- 已确认 `PROJECT_ASSISTANT_SERVICE_MCP_TOKEN` 是生产必填项，且 webapp 与 PAS 必须完全一致。
- 已确认 `webapp` 只绑定 `127.0.0.1:3000`，`project-assistant-service` 不暴露宿主机端口，公网 `/mcp` 返回 `404`。
- GitHub Actions workflow 已回写到仓库，但未因“文件存在”而被视为自动部署完成，仍待真实运行验收。
