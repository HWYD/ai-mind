# v0.4.3 Tasklist

- [x] P0：确认 v0.4.3 正式定义为 `Tool & Agent Final Turn Memory`，目标是扩展 final-turn eligibility，而不是引入 execution summary、contextEntries 或业务历史表。
- [x] P1：完成 `specs/043-tool-agent-final-turn-memory/` 全套 Spec Kit 资产，并收口 decisions、acceptance、quickstart 和 convergence 记录。
- [x] P2：在 `apps/webapp/lib/ai/runtime/chat-memory/` 新增 append-time `final-turn-adapter`、write eligibility 和 duplicate prevention guardrail。
- [x] P3：保持 persisted `ChatThreadMessage` text-only，不持久化 `source`、`turnId`、`displayKind` 或任何 raw runtime object。
- [x] P4：让普通 tool、authoritative tool、reader / utility、docs summary、MCP / resource final answer 复用既有 orchestrator final-text append 路径。
- [x] P5：让 `/tasklist` 只把 completed / blocked final answer text summary 写入 chat memory，不保存 artifact markdown、GraphState、checkpoint 或 interrupt payload。
- [x] P6：让 `/delivery-chain` 只把 completed / blocked final report text 写入 chat memory；长报告保存前按 8000 字符确定性截断。
- [x] P7：保持 `GET /api/chat/thread` DTO 与 frontend reducer public shape 兼容；刷新恢复后的 structured final turns 仍按普通 text message 渲染。
- [x] P8：保持 context builder、compaction 和 hydration 全程 text-only，不让 raw tool transcript、Tasklist runtime state、Delivery RuntimeArtifact 或 subagent raw result 进入 model context。
- [x] P9：补齐 adapter、eligibility、state、hydration、orchestrator、Tasklist、Delivery、route、frontend hydration / reducer 和 stream-core non-regression focused tests。
- [x] P10：补做 convergence follow-up，确保 Tasklist / Delivery final-turn append 也能 relay `thread-memory-status`。
- [x] P11：补做 HITL resume error experience follow-up，确保 `agent-resume` 后的 provider/runtime 失败沿用普通 chat 的错误标准化语义。
- [x] P12：完成 focused tests、stream-core tests、typecheck、lint、`build:pas`、`git diff --check` 和版本 closing 校验。

## 交付说明

- 这版保存的是“用户这轮问了什么 + 最后看到了什么”，不是执行过程历史。
- Tasklist 和 Delivery 现在也能进入同一个 chat thread history，但仍保持各自 runtime / checkpoint / artifact 边界。
- hydration 仍然只恢复普通 text messages，不新增 source badge、artifact parts 或 execution summary。

## 回归边界

- 不新增 ChatSession / ChatMessage 业务表。
- 不新增 LangGraph Store / PostgresStore。
- 不修改 `@ai-mind/stream-core` chunk union。
- 不修改 frontend reducer public shape。
- 不把 Tasklist GraphState / HITL payload 接到 chat memory。
- 不把 Delivery RuntimeArtifact / workflow progress / subagent raw result 接到 chat memory。
