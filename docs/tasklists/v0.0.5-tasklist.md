# v0.0.5 Tasklist

- [x] Major Step 1：接入单 Tool Calling
    - 新增 `calculator`。
    - 让模型在有工具时可以返回 `tool_calls`。
    - 无工具或无需工具时保持普通回答链路。

- [x] Major Step 2：补工具参数校验
    - 检查工具是否存在。
    - 对参数做归一化。
    - 使用 `Zod` 校验模型生成的工具参数。

- [x] Major Step 3：执行工具并回填结果
    - 执行通过校验的 tool call。
    - 将工具结果回填给模型。
    - 生成最终回答。

- [x] Major Step 4：前端展示 Tool part
    - 支持 `reasoning / tool / text` 三类 part。
    - 展示工具执行输入、输出和错误。
    - 保持 Streamdown 文本渲染不变。

- [x] Major Step 5：保留后续扩展位
    - 建立最小工具定义结构。
    - 保留 `resultIsAuthoritative` 等确定性结果策略字段。
    - 详细工程验证记录待补充。
