# v0.2.4 Tasklist

- [x] Major Step 1：冻结 graph-only 行为与协议基线
- [x] Major Step 2：审计旧 AgentState 的实际用途
- [x] Major Step 3：收窄领域 operation 入参边界
- [x] Major Step 4：状态机收口到 GraphState patch 语义
- [x] Major Step 5：final nodes 去除整包 adapter
- [x] Major Step 6：tasklist nodes 去除整包 adapter
- [x] Major Step 7：planning nodes 去除整包 adapter
- [x] Major Step 8：GraphState 文件职责收口
- [x] Major Step 9：graph-only 测试构造收口
- [x] Major Step 10：前端 trace 最小兼容验证
- [x] Major Step 11：文档、版本资产与最终验证

## Delivery Notes

- `/tasklist + @docs://versions/*.md` 仍只走 Graph Runtime。
- GraphState 已成为 Tasklist Agent 生产路径的内部运行态事实源。
- graph nodes 不再依赖整包 AgentState adapter 执行业务逻辑或返回 patch。
- route 继续基于显式 GraphState 字段判断。
- stream chunk、tasklist artifact、AgentTracePanel 和 Graph Debug Summary 保持兼容。
- 本版不实现 HITL、interrupt、Resume API、Run History 或数据库持久化。

## Verification

- `pnpm --dir apps/webapp test`
- `pnpm --dir apps/webapp typecheck`
- `pnpm --dir apps/webapp lint`
- `pnpm --filter @ai-mind/stream-core test`
- `pnpm --filter @ai-mind/stream-core typecheck`
- `git diff --check`
