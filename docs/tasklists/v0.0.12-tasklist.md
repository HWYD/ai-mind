# v0.0.12 Tasklist

- [x] Major Step 1：Docs Resource Boundary
    - 将本地 Resource URI 切换为 `docs://...`。
    - 将本地 MCP server 改名为 `project-docs-server`，只允许读取 `docs/**/*.md`。
    - 下线模型可见的 `local-text-read` Tool，保留并升级 `local-file-summary` Prompt。

- [x] Major Step 2：Capability-driven Tool Binding
    - 移除 `SkillDefinition.allowedTools`。
    - 通过 `capabilitySelectors` 解析本轮 active tools。
    - 让模型绑定、tool call 校验和执行共用同一份 active tool map，并保证 Resource / Prompt 不进入 model tool binding。

- [x] Major Step 3：Remote MCP Tool 标准化
    - 新增 remote MCP Tool adapter。
    - 将 `check_doc_consistency` 纳入标准 Tool Runtime。
    - 移除 capability-context 中 remote tool 写死执行分支，同时保留 remote Resource / Prompt 的最小上下文注入路径。

- [x] Major Step 4：Composer V1
    - 用 Tiptap 替换普通 textarea。
    - 接入 `/` command menu、`@` resource menu 和 inline command / resource chip。
    - 生成 `ComposerPayload`，后端消费 `/summary + @docs://...`、`@docs://...`、`@project://latest-context` 与 command hint。

- [x] Major Step 5：前端展示与回归
    - 用户消息气泡展示 inline chip。
    - Skill 命中、Prompt 参数、Resource / Tool JSON 展示进一步收口。
    - 普通问答、utility-skill、weather、docs resource、remote resource、remote prompt、remote tool 通过 smoke test。

- [x] Major Step 6：文档与版本资产
    - 更新 README。
    - 新增 release note 与 blog material。
    - 新增公开 versions / releases / tasklists 文档，并更新长期 architecture note。
