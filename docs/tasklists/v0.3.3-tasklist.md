# v0.3.3 Tasklist

- [x] P0：确认 v0.3.3 是 Level D 治理 / 工具链版本，不新增业务功能。
- [x] P1：创建 `specs/033-spec-kit-full-skills-default-entry/` 五件套。
- [x] P2：完成 clarify / checklist / analyze bootstrap gate。
- [x] P3：审计 v0.3.2 lightweight pilot skills，并迁移有价值规则。
- [x] P4：引入 official Spec Kit full skills，并同步依赖的 `.specify` baseline。
- [x] P5：更新 ADR、architecture docs、AGENTS 和 specs 索引。
- [x] P6：新增公开版本资产，并更新 README / package version。
- [x] P7：完成最终 verification、converge 或人工等价收口。

## 交付说明

- v0.3.3 不修改 runtime、Graph、Prisma schema、stream protocol、API route 或前端 reducer。
- 本版将 `speckit-*` 命名空间收口为 official full skills。
- AI Mind 项目规则通过 constitution、specs、ADR、architecture docs、template overrides 和 AGENTS 生效。
- `speckit-converge` 进入 Level C / D 收口检查。
- official prerequisites 需要当前 feature context；AI Mind 可显式设置 `SPECIFY_FEATURE_DIRECTORY` 指向当前 `specs/<version-topic>/`。
- 新 Codex 会话完整触发 `$speckit-specify` / `$speckit-plan` / `$speckit-tasks` 等仍建议作为人工补验项。
