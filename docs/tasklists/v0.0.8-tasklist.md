# v0.0.8 Tasklist

- [x] Major Step 1：切换 Runtime 主线
    - 移除当前版本主线中的 `writer-skill`。
    - 新增 `reader-skill`。
    - 保持 `utility-skill` 继续稳定。

- [x] Major Step 2：落地 reader Tool
    - 新增 `city-weather`。
    - 新增 `local-text-read`。
    - 保持本地文件读取安全边界。

- [x] Major Step 3：更新路由与前端模式
    - 自动模式支持天气和文件读取高置信命中。
    - Skill 模式改为自动、实用、读取。
    - 输入提示文案按模式区分。

- [x] Major Step 4：统一前端组件基线
    - 建立 `shadcn/ui` 基线。
    - 使用 `radix-vega`、Radix、lucide-react 和 CSS variables。
    - 统一输入区、错误条、推理面板、Tool 卡片和空状态。

- [x] Major Step 5：微调交互细节
    - 收紧输入框和推理面板密度。
    - 区分 Tool 状态颜色。
    - 保持模型选择器和深度思考开关可用。

- [x] Major Step 6：文档与验证
    - 同步 README、release、runtime note 和 blog material。
    - 验证 reader-skill、utility-skill 和普通聊天主链。
