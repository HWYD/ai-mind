# v0.2.1 Tasklist

- [x] Major Step 1：Model Catalog 与 Provider 配置
    - 建立服务端模型白名单、稳定 `modelId` 和能力声明。
    - 支持 Ollama、Qwen、DeepSeek 配置并保持 API Key 仅服务端可见。

- [x] Major Step 2：公开模型列表与请求协议
    - 新增 `GET /api/ai/models`。
    - 请求统一使用 `options.modelId`，非法模型 fail closed。

- [x] Major Step 3：Ollama Provider 等价迁移
    - 抽出 ModelProvider / Registry 和统一模型 handle。
    - 普通聊天、Tool Calling 和 tasklist Agent 不再直接创建 `ChatOllama`。

- [x] Major Step 4：Qwen / DeepSeek Provider
    - 建立 OpenAI-compatible Provider 基础层。
    - 接入 Qwen 与 DeepSeek 白名单模型。

- [x] Major Step 5：Provider 错误与安全边界
    - 新增 Provider 错误标准化和中文用户提示。
    - 原始错误只进入脱敏日志，不进入前端和 stream。

- [x] Major Step 6：输入输出与限流
    - 增加输入字符、输出 token 和请求超时限制。
    - 默认开启 IP / HttpOnly session 的进程内每日限流。

- [x] Major Step 7：前端模型选择器
    - 首屏从服务端事实源初始化模型列表。
    - 按“线上模型 / 本地模型”分组，只提交 `modelId`。

- [x] Major Step 8：P0 回归
    - 回归普通聊天、Tool Calling、Skills 和 tasklist legacy / graph 主链。
    - 验证模型切换不改变 Agent 权限边界。

- [x] Major Step 9：真实云模型与 usage
    - 完成 Qwen / DeepSeek 普通聊天和 tasklist smoke。
    - 增加 usage / token best-effort 脱敏观测。

- [x] Major Step 10：版本资产收口
    - 同步配置模板、README、runtime note、release、公开 docs 和版本号。
    - 完成测试、typecheck、lint、构建与敏感信息检查。
