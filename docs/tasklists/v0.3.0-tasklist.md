# v0.3.0 Tasklist

- [x] Step 0：正式方案与实现边界收口。
- [x] Step 1：稳定 HITL / AgentRun / stream 契约。
- [x] Step 2：固定 TasklistStrategy 结构化字段。
- [x] Step 3：引入 Prisma / PostgreSQL / AgentRun schema。
- [x] Step 4：验证 LangGraph durable checkpointer。
- [x] Step 5：实现 AgentRunService 与 resume 防护。
- [x] Step 5.5：Prisma 上移为共享 `@ai-mind/database` package。
- [x] Step 6：新增 Strategy Review graph node 与 route。
- [x] Step 7：新增条件式 Tasklist Revision Review。
- [x] Step 8：接入生产 runner / coordinator / resume API。
- [x] Step 9：接入 stream chunk 与同 assistant message 续写。
- [x] Step 10：实现 HumanReviewComposerPanel 与两类审核卡。
- [x] Step 11：移除刷新后 pending HITL 恢复。
- [x] Step 12：前端 HITL 交互与视觉收口。
- [x] Step 13：本地、CI、生产数据库交付。
- [ ] Step 14：全量验证、浏览器 smoke、文档和版本资产。

## Delivery Notes

- Strategy Review 必停。
- Tasklist Revision Review 仅在 `warningDisposition.fixNow.length > 0` 时触发。
- 最多两轮受控修订。
- resume 后不创建新的 assistant message。
- 刷新后不恢复 pending HITL。
- 数据库层位于 `packages/database`，供 webapp 与未来 project-assistant-service 共用。

## Verification

- database generate / validate / tests 通过。
- webapp tests / typecheck / lint / build 通过。
- stream-core tests / typecheck / build 通过。
- Docker image build 通过。
- 容器内 migration / checkpointer setup 通过。
- Docker webapp health / page smoke 通过。
- 浏览器级 HITL smoke 当前被自动化环境安全策略阻止，需要人工复验。
