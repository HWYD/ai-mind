# v0.0.10 Tasklist

- [x] Major Step 1：收口 Chat Runtime 边界
    - 保持 `chat-service` 为薄 facade。
    - 将 session、orchestration、stream consumption 和 tool runtime 职责下沉到 `runtime/`。
    - 保持公开 chat service 入口稳定。

- [x] Major Step 2：统一流式错误与生命周期
    - 使用单一 `error` chunk 结构。
    - 增加可选错误阶段信息。
    - 确保 stream start 和终态事件只写出一次。

- [x] Major Step 3：将 authoritative answer 升级为 runtime 策略
    - 集中处理确定性工具结果直出逻辑。
    - 对混合问题和开放式问题保持保守。
    - 使用聚焦测试覆盖该行为。

- [x] Major Step 4：抽取 stream-core package
    - 新增 `@ai-mind/stream-core`。
    - 下沉协议类型、生命周期、错误 helpers、static writers 和 NDJSON writer。
    - 暴露 root、protocol、web 三个 package 入口。

- [x] Major Step 5：webapp 接入 workspace package
    - 通过 workspace dependency 消费 `@ai-mind/stream-core`。
    - 配置 package transpilation 与 module resolution。
    - 保持 app runtime 业务编排不进入 package。

- [x] Major Step 6：对齐测试与文档
    - Webapp 测试保留在 app 测试目录。
    - Stream-core 测试保留在 package 测试目录。
    - 更新版本文档、runtime note、release note 和 blog material。
