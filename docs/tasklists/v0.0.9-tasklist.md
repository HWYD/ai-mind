# v0.0.9 Tasklist

- [x] Major Step 1：建立 MCP Host 基础层
    - 新增本地 MCP server 静态 registry。
    - 新增 client manager、transport 与 adapter 边界。
    - 避免 MCP 细节进入 `chat-service` 和 Skill 定义。

- [x] Major Step 2：天气接入 MCP Tool
    - 新增带 `get_weather` 的 `weather-server`。
    - 保持模型可见能力名 `city-weather`。
    - 继续通过现有 Tool card 展示天气执行结果。

- [x] Major Step 3：项目文件接入 MCP Resource
    - 新增 `project-files-server` Resource 读取。
    - 保持模型可见能力名 `local-text-read`。
    - 保留严格本地文件访问边界。

- [x] Major Step 4：新增 Resource 协议与 UI 支持
    - 新增 Resource 流式事件。
    - 新增 Resource message part 与 Resource card。
    - 为 Tool 和 Resource 展示 source 与 server 信息。

- [x] Major Step 5：稳定 reader Skill 路由
    - 保持 `reader-skill` 作为 MCP capability 消费层。
    - 保持 `utility-skill` 使用 internal tools。
    - 保留普通聊天 fallback 行为。

- [x] Major Step 6：同步版本资产
    - 对齐 README、runtime note、release note 和 blog material。
    - 保持本版本定位为 MCP Host MVP。
