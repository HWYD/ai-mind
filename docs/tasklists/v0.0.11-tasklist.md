# v0.0.11 Tasklist

- [x] Major Step 1：新增 Capability Model 与 Skill Metadata
    - 定义 Tool、Resource、Prompt 的 capability 字段。
    - 新增结构化 Skill metadata 与 capability selectors。
    - 保持 capability model 只统一描述层，不统一执行链。

- [x] Major Step 2：新增 Local Prompt capability
    - 新增 `local-file-summary`。
    - 通过 local MCP surface 暴露为 Prompt capability。
    - 将固定 prompt 参数注入最终回答上下文。

- [x] Major Step 3：新增 Remote MCP 单 server 闭环
    - 新增 `apps/project-assistant-service`。
    - 暴露 mock Resource、Prompt、Tool capability。
    - 使用 Streamable HTTP 与 mock 鉴权。

- [x] Major Step 4：新增 runtime capability 消费
    - 解析固定 `reader-skill` remote capability 场景。
    - 通过 runtime bridge 执行 Resource、Prompt、Tool capability。
    - 写出执行事实，并将结果注入最终回答上下文。

- [x] Major Step 5：新增前端 Skill 与 capability 执行事实
    - 展示 Skill 命中。
    - 展示 Tool、Resource、Prompt 执行事实。
    - 展示 source、location、server、status 和 errors。

- [x] Major Step 6：验证关键场景
    - 验证 remote resource、remote prompt、remote tool、local file summary 和普通聊天。
    - 验证 capability 错误进入统一流式错误协议。
