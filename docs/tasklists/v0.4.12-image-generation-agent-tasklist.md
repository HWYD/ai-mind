# v0.4.12 Tasklist：Image Generation Agent

- [x] P0：定义 `/image` 显式入口、固定模型/endpoint 与单张文生图边界。
- [x] P1：实现受控 LangGraph ImageBrief/Prompt 图，限制规划调用、一次修正和一次 Provider 生成。
- [x] P2：实现 ImageGenerationRun、幂等/取消/临时过期，以及受所有权保护的同源图片内容路由。
- [x] P3：扩展安全 stream parts 和消息 UI，展示 ImageBrief、进度、临时预览与下载。
- [x] P4：补齐 Graph、Provider、Repository、路由、reducer、UI 与 120 秒处理中状态的验证。
- [x] P5：同步 ADR、架构、版本和发布说明，并以固定 Provider smoke 检查外部契约漂移。
- [x] P6：增加独立生图每日配额（Session 3、IP 默认 10，支持 10–20 配置）并补齐边界回归。

## 回归边界

- 普通聊天、Tool、Skill、MCP、Tasklist Agent 和 Delivery Chain 不会因非显式请求进入图片链路。
- 本版不把图片编辑、对象存储、成本估算或 HITL 纳入实现范围。
