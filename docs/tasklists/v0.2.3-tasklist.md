# v0.2.3 Tasklist

- [x] Major Step 1：冻结外部协议与 Graph-only 测试基线
- [x] Major Step 2：Graph Runtime 成为 `/tasklist` 唯一执行入口
- [x] Major Step 3：移除 runtime switch / fallback
- [x] Major Step 4：GraphState 字段审计与 JSON-serializable 检查
- [x] Major Step 5：GraphState 分区与 node patch 收口
- [x] Major Step 6：Graph routes 基于显式 state 字段
- [x] Major Step 7：删除 legacy dead code
- [x] Major Step 8：测试结构 graph-only 收口
- [x] Major Step 9：文档、版本资产与最终验证

## Delivery Notes

- `/tasklist + @docs://versions/*.md` 只走 Graph Runtime。
- legacy runner、legacy runtime wrapper 和 runtime selector 已删除。
- 历史 `AI_MIND_TASKLIST_AGENT_RUNTIME` 已不再影响 Tasklist Agent 执行路径。
- GraphState 已拆掉外层 `agentState` 包裹，按 Graph Runtime 分区承载运行态。
- stream chunk、tasklist artifact、AgentTracePanel 和 Graph Debug Summary 保持兼容。
- 本版不实现 HITL、Durable Agent Run、Resume API、Run History 或数据库持久化。

## Verification

- `pnpm --dir apps/webapp test`
- `pnpm --dir apps/webapp typecheck`
- `pnpm --dir apps/webapp lint`
- `pnpm --filter @ai-mind/stream-core test`
- `pnpm --filter @ai-mind/stream-core typecheck`
- `git diff --check`
