# v0.0.7 Tasklist

- [x] Major Step 1：扩展请求协议
    - `ChatRequest.options` 支持 `skill`。
    - 保持不传 Skill 的旧路径兼容。
    - 非法 Skill 有明确处理。

- [x] Major Step 2：新增 Skill 定义层
    - 新增 Skill Definition。
    - 新增 Skill Registry。
    - 注册默认 `utility-skill`。

- [x] Major Step 3：Runtime 接入 Skill
    - 读取请求中的 Skill。
    - 按 Skill 的 `allowedTools` 过滤 active tools。
    - 将 Skill prompt 注入主链路。

- [x] Major Step 4：接入 unit-convert
    - 支持长度、重量和温度换算。
    - 提供 schema、参数归一化和展示格式。
    - 设置确定性结果优先。

- [x] Major Step 5：默认启用 utility-skill
    - `/` 默认进入实用 Skill。
    - 不新增 Skill UI。
    - 保持普通开放式问答可用。

- [x] Major Step 6：回归验证与文档同步
    - 验证普通问答和四个实用工具。
    - 验证 tool error 不污染下一轮。
    - 同步 runtime note、release note 和博客材料。
