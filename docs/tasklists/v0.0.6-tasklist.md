# v0.0.6 Tasklist

- [x] Major Step 1：升级 Tool Registry
    - 从单工具结构扩展为多 Tool Registry。
    - Runtime 通过统一接口查询 active tools。
    - 每个 Tool 内聚 schema、执行和展示信息。

- [x] Major Step 2：接入 datetime
    - 支持当前时间、日期加减和星期判断。
    - 验证第二类确定性 Tool。
    - 保持 calculator 不回归。

- [x] Major Step 3：接入 text-transform
    - 支持 Markdown 转纯文本、链接提取、代码块提取、JSON 格式化。
    - 验证单 Tool 多 action 设计。
    - 输出可直接作为 Tool 结果展示。

- [x] Major Step 4：增强 Tool 展示与错误兜底
    - Tool part 展示 title、action、input、output 和 error。
    - 工具未注册、参数非法、执行失败时输出结构化错误。
    - 流式取消和关闭过程安全收束。

- [x] Major Step 5：验证多 Tool 稳定性
    - 验证普通问答、calculator、datetime、text-transform。
    - 验证多轮后工具状态不污染下一轮。
    - 验证 typecheck 与 build。
