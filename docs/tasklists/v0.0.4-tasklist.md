# v0.0.4 Tasklist

- [x] Major Step 1：收敛本地聊天链路
    - 使用 `LangChain.js + Ollama` 承载本地模型调用。
    - 保持 `/api/chat` 为自定义服务端入口。
    - 不引入额外重型聊天运行时。

- [x] Major Step 2：建立流式协议
    - 使用 NDJSON 输出流式 chunk。
    - 定义 start、text、finish、error 等最小事件。
    - 为后续 reasoning 和结构化 part 预留扩展。

- [x] Major Step 3：建立消息 parts 模型
    - 使用 `typed parts` 组织 assistant 消息。
    - 首版重点支持 text，保留 reasoning 扩展位。
    - 前端按 part 渲染内容。

- [x] Major Step 4：接入前端流式状态
    - 使用自定义 `useChatStream`。
    - 支持消息合并、增量渲染和取消请求。
    - 保持多轮上下文的最小实现。

- [x] Major Step 5：统一校验与渲染
    - 使用 `Zod` 校验请求和 stream chunk。
    - 使用 `Streamdown` 渲染流式 Markdown。
    - 详细工程验证记录待补充。
